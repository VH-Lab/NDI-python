def epoch(session):
    """
    Generate a table of all 'epoch' documents.
    Note: NDI doesn't typically store 'epoch' as documents in the same way as elements,
    but sometimes epoch records are stored.

    This function seems to be a placeholder or specific to certain epoch docs.
    """
    # Placeholder implementation based on pattern
    raise NotImplementedError("ndi.fun.doc_table.epoch not fully ported yet.")
